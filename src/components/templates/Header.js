import Routes from '../../Routes';
import Nav from '../organisms/Nav/Nav';

export default function Header() {
    return(
        <div>
            <Nav/>
            <Routes />
        </div>
    );
}